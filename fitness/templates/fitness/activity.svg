{% load static %}

<!DOCTYPE html>
<html>
    <head>
        <link href="{% static 'fitness/cyborg_extra.css' %}" rel="stylesheet">
    </head>
<body>
    <svg height="30" width="30">
        <polyline class="activity-polyline"
            points="
            {% for x, y in object.svg_points %}
                {{x}}, {{y}}
            {% endfor %}
            "
            style="fill:none;stroke-width:1"
        />
    </svg>
</body>
</html>